[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Serve index.html for every route so React Router handles deep links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
